import { Tela } from "@/components/ui/tela";

// Primeiro acesso pelo Google: escolha entre Jogador e Olheiro (RF-12)
export default function EscolherPerfil() {
  return <Tela titulo="Como você vai usar o NextPass?" />;
}
