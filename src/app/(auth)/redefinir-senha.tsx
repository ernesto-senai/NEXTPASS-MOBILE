import { Tela } from "@/components/ui/tela";

// Aberta pelo link do e-mail: nextpass://redefinir-senha?token=... (RF-05)
export default function RedefinirSenha() {
  return <Tela titulo="Nova senha" />;
}
