import { Tela } from "@/components/ui/tela";

// Responsável cria a conta pelo link do e-mail e confirma o vínculo (RF-48)
export default function ConfirmarVinculo() {
  return <Tela titulo="Confirmar vínculo" />;
}
