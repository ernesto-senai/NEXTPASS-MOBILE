import { Tela } from "@/components/ui/tela";

// Escolha da avaliação e mensagem de até 300 caracteres (RF-39, RF-40, RN-10)
export default function ChamarParaAvaliacao() {
  return <Tela titulo="Chamar para avaliação" />;
}
